This directory is basically a bunch of vomit that represents a scratch pad of
where I tested particular things of xgbutil as I developed it.

None of the code in this directory should ever be looked at. You shouldn't even 
expect it to compile.

It should (and will) be deleted at some point.

